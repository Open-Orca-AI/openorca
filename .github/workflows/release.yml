name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - rid: win-x64
            artifact: openorca-win-x64
            ext: zip
          - rid: linux-x64
            artifact: openorca-linux-x64
            ext: tar.gz
          - rid: osx-x64
            artifact: openorca-osx-x64
            ext: tar.gz
          - rid: osx-arm64
            artifact: openorca-osx-arm64
            ext: tar.gz

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Publish
        run: >
          dotnet publish src/OpenOrca.Cli
          -c Release
          -r ${{ matrix.rid }}
          --self-contained
          -p:PublishSingleFile=true
          -o publish/${{ matrix.rid }}

      - name: Package (zip)
        if: matrix.ext == 'zip'
        run: cd publish/${{ matrix.rid }} && zip -r ../../${{ matrix.artifact }}.zip .

      - name: Package (tar.gz)
        if: matrix.ext == 'tar.gz'
        run: tar -czf ${{ matrix.artifact }}.tar.gz -C publish/${{ matrix.rid }} .

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.${{ matrix.ext }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/**/*
